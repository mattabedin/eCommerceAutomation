/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router is the default in Next.js 14 — no extra config needed.
  // Phase 3 will add storefront rewrite rules here.
};

export default nextConfig;
